<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Centering the professional neon logo with proper padding -->
    <!-- Safe zone is usually 72dp inside 108dp, so we use 70dp for a 'perfect' fit -->
    <item
        android:drawable="@drawable/screen"
        android:gravity="center"
        android:width="60dp"
        android:height="60dp" />
</layer-list>